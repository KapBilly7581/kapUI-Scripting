import { Str } from "../../values/Str";

export interface TooltipTriggers {
    tooltip_name?: Str,
    tooltip_top_content_control?: Str,
    tooltip_bottom_content_control?: Str,
    tooltip_area?: Str,
}